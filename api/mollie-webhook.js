const REPO = process.env.GITHUB_REPO || 'LethalKrisixTools/rpmfest';
const BRANCH = process.env.GITHUB_BRANCH || 'main';
function json(res,status,body){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.end(JSON.stringify(body))}
async function githubRequest(pathname,options={}){if(!process.env.GITHUB_TOKEN)throw new Error('Falta GITHUB_TOKEN.');return fetch(`https://api.github.com/repos/${REPO}/contents/${pathname}`,{...options,headers:{Accept:'application/vnd.github+json',Authorization:`Bearer ${process.env.GITHUB_TOKEN}`,'X-GitHub-Api-Version':'2022-11-28','Content-Type':'application/json',...(options.headers||{})}})}
function b64(text){return Buffer.from(text,'utf8').toString('base64')}
module.exports=async function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'Método no permitido'});try{let body=req.body;if(typeof body==='string')body=body.startsWith('{')?JSON.parse(body):Object.fromEntries(new URLSearchParams(body));const paymentId=body?.id;if(!paymentId)return json(res,400,{error:'Missing payment id'});if(!process.env.MOLLIE_API_KEY)throw new Error('Falta MOLLIE_API_KEY.');const paymentResponse=await fetch(`https://api.mollie.com/v2/payments/${encodeURIComponent(paymentId)}`,{headers:{Authorization:`Bearer ${process.env.MOLLIE_API_KEY}`}});const payment=await paymentResponse.json();if(!paymentResponse.ok)throw new Error(payment?.detail||'No se pudo consultar el pago.');if(payment.status!=='paid')return json(res,200,{ok:true,status:payment.status});const items=payment.metadata?.items||[];if(!items.length)return json(res,200,{ok:true,noItems:true});const storeResponse=await githubRequest('data/store.json');if(!storeResponse.ok)throw new Error(`GitHub store read failed: ${storeResponse.status}`);const storeFile=await storeResponse.json();const store=JSON.parse(Buffer.from(storeFile.content.replace(/\n/g,''),'base64').toString('utf8'));for(const item of items){const product=(store.products||[]).find(p=>String(p.id)===String(item.id));if(product&&Number.isFinite(product.stock)&&product.stock>=0)product.stock=Math.max(0,product.stock-Number(item.qty||0))}await githubRequest('data/store.json',{method:'PUT',body:JSON.stringify({message:`feat: actualizar stock ${payment.metadata?.orderId||payment.id}`,content:b64(JSON.stringify(store,null,2)+'\n'),sha:storeFile.sha,branch:BRANCH})});return json(res,200,{ok:true,status:payment.status})}catch(e){console.error(e);return json(res,500,{error:e.message||'Webhook error'})}};
